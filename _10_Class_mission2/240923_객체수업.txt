클래스 정의

클래스의 의존관계를 파악하고 관련 변수 설정
의존하는 객체를 참조하기 위한 방법 > 직접 객체를 만들어서 

각 클래스의 필요한 변수와 메서드 정의를 함

기능 구현


메소드

1. 메소드의 의미는 기능을 제공한다.

2. 메소드는 정의와 호출을 구분한다.
   메소드의 정의 = 메소드를 만든다.
   메소드는 클래스 내부에 포함된다.
   메소드의 호출 = 메소드를 사용한다.

3. 만드는 사람 입장 / 사용하는 사람 입장

void 
호출한 쪽에 기능수행 후 데이터를 넘겨줄 수 없다.

public String getTnum() {
	return tNum
}


String mnum = tlist[i].getTnum();
if(mnum.equls(modNum)){
}

if(tlist[i].tnum.equls(modNum)){
}



클래스 = 메서드 멤버변수 구성

클래스 나누기 (설계하기) 세분화

클래스 간의 의존관계를 생성 
멤버변수를 결정

주소값을 직접 만들것인가 누군가에게 받을것인가(아직 하지않음)

클래스 코딩

+

객체에 있는 멤버변수를 직접 접근하지 못하게 ..
보호하다..

직접 변수에 접근하지 않고 클래스에 부탁

메소드를 정의한다 = 메소드를 만든다.

[자원의 은닉화]


