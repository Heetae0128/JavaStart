반복문에서 break continue 키워드 사용

break - 현재 반복문을 종료한다.

반복문이 실행되지 않고 if문만 있다면 실행되지 않는다.

continue - 더 이상 코드를 실행하지 않는다.
		 증감식 > 조건판별
	 (증가,감소하는식)
	 for 조건식과 증감식을 거친다.
	 
	 Q. 1부터 10까지 숫자 중 짝수의 합 continue 한번 사용
	 
		 int sum = 0;
		 for(int i=1; i < 11; i++){
		 	if(i%2 ==1);
		 	contunue
		 	}
		 	sum+=i;
		 	sysout(sum)
		 	
String(자료형) id(변수) : "abcdefee"(값) (id에 알파벳 e는 금지)

자료형 원시 : '.'사용불가 종류:int char boolean float
	 참조 : '.'사용가능 종류:String
	 
	 id 참조자료형(문자열) > index가 있고 0부터시작
	 참조자료형에는 .length() / .charAt() 사용할수있다 	
	 .length() - 길이
	 .charAt() - 매개변수의 위치의 문자 리턴
	 ex) i=1; 문자 b를 리턴
	 소괄호가 있으면 메소드다.
	 
	 
	 
	시나리오
	String id="abcdefee"
	boolean flag = false;
	
	for(int i = 0; i < id.length(); i++){
		if(id.charAt(i)=='e'){
			flag = true;
			break;
		}
	}
		if(flag){
			sysout("e포함됨");
		}
		
		
	자료형의 참조 type : String  Random Scanner
	
	
	객체 : class(설계도)로 객체(건물)를 만든다.
	String(class) /id = /new(만든다) /String("abc")(건물:객체)
	
	객체 : 부품 (변수와 메소드로 구성되어있다.)
	
	변수 : 저장
	메소드 : 기능	
	