

-쭈꾸미 레시피는 실제 사람이 먹을 음식이라고 할 수 있을까?

아니요, 문제에서의 질문은 먹을 음식이다?라고말한게 아닌가?
먹을 수 없다 

-레시피는 왜 만들어 졌을까?

사람들이 똑같이 사용하기 위해? 과정이 필요해서?
다시 사용하기 위해(재사용성)

-레시피와 실제 만들어진 음식은 어떤 차이가 있을까?

레시피는 목적을 가지고 그 목적은 음식이다

사용법
실제 사용하기 위한 대상(실제값)

-레시피는 1개만 있고 레시피를 통해 만들어 진 음식도 1개라고
말 할 수 있을까?

클래스를 위해 만든 객체는 서로 독립적이다.
재사용성
여러개의 음식을 만들 수 있다.

-레시피가 있다면 음식도 만들어 진 것일까?


-서로 다른 쭈꾸미 레시피로 각각 요리를
했다면 음식은 쭈꾸미라는 성격을 갖는다.
이 음식은 서로 같은 음식이라고 할 수 있을까?

서로 다른 쭈꾸미 클래스로 객체는 쭈꾸미라는 성격을 갖는다(변수)
이 객체는 서로 같은 객체라고 할 수 있을까?
아니요 이 음식(객체)는 서로 다른 레시피(클래스)로 이루어져 있고,
독립적이기에 같다고 할 수 없다.

-레시피는 여러 사람과 <공유>할 수 있다, 이 때 레시피는
하나이고 레시피를 참조하는 사람도 1명 일까?

클래스는 공유할 수 있다, 이때 클래스는 하나이고
클래스를 참조하는 변수도 1개일까?
참조 하는 변수는 꼭 하나만 있지는 않는다.

<공유의 장점>
자원의 낭비를 줄일 수 있다.
효율적이다.
ex)화장실 한개로 여러사람이 사용할 수 있다.?

메모리의 사용량을 적게 만들 수 있다.
레시피는 퍼블릭(공공의)한 성격이 있다.
어떤사람이 내용을 수정하면 내용을 공유한다.


-여러 사람이 하나의 레시피를 참조 할 때, 어떤
사람이 레시피를 수정한다면 다른 사람은 수정된 레시피를 볼까?
아니 수정되지 않은 상태의 레시피를 볼까?

int = 4byte
변수는 값을 저장하는 공간(메모리)
원시,참조변수

원시변수 : 내가 원하는 값

참조변수 : 내가 원하는 객체가 있는 주소

. < 참조하라 

1. 클래스 정의하고 객체를 만든다.
2. 변수는 원시변수(실제값) 참조변수(주소) 
3. 참조변수는 객체의 주소값을 참조한다.
   객체의 주소는 힙에 있다.




